db.createUser(
    {
        user: "<DATABASE_USER>",
        pwd: "<DATABASE_PASS>",
        roles: [
            {
                role: "readWrite",
                db: "<DATABASE_NAME>"
            }
        ]
    }
);